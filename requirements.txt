# Core (bắt buộc)
opencv-python>=4.5.0
numpy>=1.21.0
python-dotenv>=0.19.0
keyboard>=0.13.0

# Tối ưu capture (tùy chọn — bot vẫn chạy được nếu không cài)
# Cài để tăng tốc capture 10-50x:  pip install scrcpy-client
# scrcpy-client>=0.4.0
